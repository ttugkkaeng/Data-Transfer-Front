import { returnJsonType, PostReturnJsonType } from './Types';

export function mappingViewData(PostReturnJson: PostReturnJsonType): returnJsonType {
  const tempArray = [];
  const mappingContent = PostReturnJson.content.map((item, index) => {

  })

}
